---
description: Scaffold a new Loadsmith config-provider plugin (URI → config bytes)
argument-hint: <name> [notes]
allowed-tools: [Read, Write, Edit, Bash]
---

The user invoked this command with: $ARGUMENTS

Read and follow the full instructions in
[`.agents/skills/create-config-provider-plugin.md`](../../.agents/skills/create-config-provider-plugin.md).
The real, agent-agnostic logic lives there so the knowledge base stays
centralized in `.agents/`.
